// This file can be used to export shared types across the application.
// For now, specific types like ServerConfig are defined in zod-schemas.ts and can be imported from there.

// Example:
// export type { ServerConfig } from '@/lib/zod-schemas';

// Add other global types here if necessary.
export {}; // Placeholder to make it a module
